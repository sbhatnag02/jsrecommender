// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyCuPQgDDfCttdnGE-XmE9KxPt_QCmGHavA',
    authDomain: 'js-recommender.firebaseapp.com',
    databaseURL: 'https://js-recommender.firebaseio.com',
    projectId: 'js-recommender',
    storageBucket: 'js-recommender.appspot.com',
    messagingSenderId: '273629354899',
    appId: '1:273629354899:web:5eafaed2298459e4'
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
